app:
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - dashboard.example.com
    tls:
      enabled: true
      secretName: dashboard-tls
auth:
  mode: token
metricsScraper:
  enabled: true
kong:
  enabled: false
